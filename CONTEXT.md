# 今のタスク

フォロワー増加戦略の実装中。X投稿へのハッシュタグ自動付与を完了。次はIG側の改善を検討中。

# 直近の変更

- `scripts/auto-publish/x-variety-single.ts` — hashtag-strategy.md を読み込んでプロンプトに埋め込み。X バイラル投稿にハッシュタグ3-5個が自動付与されるようになった
- `scripts/auto-publish/generate-x-post.ts` — 同上。ブログ連動 X スレッドにもハッシュタグ自動付与
- `src/app/api/cron/daily-publisher/route.ts` — `.find()` → `.filter()` に修正。同日に同タイプ（blog x 2件など）が複数ある場合に全件処理されるようになった

# 試したけどダメだったこと

- (まだなし)

# 次にやること

1. IG キャプション生成にハッシュタグ15個の自動付与を組み込む（reels-factory 側の対応が必要）
2. post-patrol の実装強化 — 投稿後エンゲージメント率の自動計測と低パフォーマンスパターン検出
3. x-monitor のデータを次回テーマ選定にフィードバックする仕組み

# 未解決の疑問・迷い

- IG ハッシュタグ15個の組み込みは reels-factory 側のプロンプト改変になる可能性 → 取締役確認が必要か？
- blog→X 連動は既に動いているが、X→IG のクロスポスト（カルーセル画像をXにも投稿）は効果ありそうか？

# 最終更新

2026-04-23 (作業中)
